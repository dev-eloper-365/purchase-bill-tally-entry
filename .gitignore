# Real purchase bills - GSTINs, bank account numbers, invoice amounts
Bill_Samples/

# Real Tally request/response logs from testing - contains real financial data
logs/

# Node dependency tree for the tools/ Node-based test harness - regenerable
# via `npm install` in tools/, not meant to be committed
tools/node_modules/

# Scratch output from testing the OCR pipeline - includes real extracted
# bill content (GSTIN, amounts), same reason Bill_Samples/ is excluded
tools/eng.traineddata
tools/*.txt
tools/*.png
